import styled from 'styled-components';

import {
  background,
  margin,
  padding,
  flexbox,
} from '~/components/theme/styled';

export const Box = styled('div')`
  ${background}
  ${padding}
  ${margin}
  ${flexbox}
`;
